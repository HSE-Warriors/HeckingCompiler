#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include "Test.h"
#include "Test1.h"
#include "Test2.h"

void f1();
int f2();
int f();
void f3(int* b);
int recursive_function(int n);
int Main();

int f()
{
	return 5;
}

void f3(int* b)
{
	*b = 10;
	f2();
}
int FunctionWithCorrectName(); //correct name, used
int functionWithIncorrectName(); //incorrect name, used
int UnusedFunctionWithCorrectName(); //correct name, unused
int unusedFunctionWithIncorrectName(); //incorrect name, unused

int abc = 0;
int c = 100;
int main()
{
	float AsdfQwerty = -123.214; double ii = -1.1; int num = -00000000.0000; char Char = 0;
	int array[MAX_SIZE] = { 0 }; int n = 0, m = 0; printf("Enter size of array:\n"); scanf("%d", &n); for (int i = 0; i < n; ++i) { scanf("%d", &array[i]); }for (int i = 0; i < n; ++i) {
		if ((i == 0) && (array[i] < array[i + 1])) { m++; }if ((array[i] < array[i - 1]) && (array[i] < array[i + 1])) { m++; }
		printf("%d %x\n", c, &c);
		//printf("Hello");
		while (!0)
		{
			if (1)
			{
				break;
			}
		}
		int
			a
			=

			5;
		int b = f();
		int c;
		c
			=
			abc;
		printf
		("%d %x\n", c, &c);
		//f1(&c);
		//a += c;
		while (a)
		{
			int c = 50;
			printf("%d %x\n", c, &c);
			c += 10;
			printf("%d %x\n", c, &c);
			//fscanf(stdin, "%d", &b);
			//f1(&b);
			//a += b;
			if (b = 10)
				break;
			while (!NULL)
			{
				if (1)
					break;
			}
		}
		for (int i = 0; i < 10; i + 1)
		{
			int efef = 0;
			efef += 3;
			break;
			char p[5] = "0";
			if (1)
				break;
		}
		for (int i = 0; i < 2; ++i); for (int i = 6; i > 5; ++i)
			printf("%d %x\n", c, &c);

		a =
			10;
		a
			+=
			b
			;               f1();
		printf("%d", a);
		for (int i = 0; i < 100000000; ++i)
		{
			a++; //ffefef/f/ef/e/f/e/fe/f/fe/fe/f/e/f/ef/

		}
		int correctlyNamedEmptyVariable; //correct name, not initialized
		int correctlyNamedInitializedVariable = 10; //correct name, initialized, used
		int IncorrectlyNamedEmptyVariable; //incorrect name, not initialized
		char IncorrectlyNamedInitializedVariable[] = "hi"; //correct name, initialized
		int inited;

		printf("Enter name of the file:\n");
		scanf_s("%s", nameOfFile, 255);

		correctlyNamedInitializedVariable++;

		FunctionWithCorrectName();
		functionWithIncorrectName();

		inited = 0;
		int superb;
		/*int x = 0;

		while (x)
		{
			;
		}*/
		func0(num);
		while (AsdfQwerty < 0) { --AsdfQwerty; } while (ii) {
			//ii = ii + 3;
		}
		while (num)
		{
			int efef = 0;
			efef += 3;
			char p[5] = "0";
			scanf("%d", num);
		}
		while (Char)
		{
			int efef = 0;
			efef += 3;
			//scanf("%f", &ii); 
			Char = getchar();
			char p[5] = "0";
			++Char;
		}
		int aInFile /*efefef*/ = 10;
	}
}


void f1()
{
	f3();
	printf("Hello, world!\n");
	f2();
}

int f2()
{
	static int count = 30;
	f1();
	return ++count;
}

//void f4();
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>
#include <stdlib.h>
//1wqrwqw152
#define MAX_SIZE 1000
int recursive_function(int n) 
{ 
	if (n == 0 || n == 1)
		return 1; 
	return n * recursive_function(n - 1); 
}
int Main()
{ 
	int array[MAX_SIZE] = { 0 };
	int n = 0, m = 0; 
	printf("Enter size of array:\n"); 
	scanf("%d", &n); 
	for (int i = 0; i < n; ++i) 
	{ 
		scanf("%d", &array[i]);
	}
	for (int i = 0; i < n; ++i) 
	{ 
		if ((i == 0) && (array[i] < array[i + 1]))
		{ 
			m++; 
		}
		if ((array[i] < array[i - 1]) && (array[i] < array[i + 1])) 
		{ 
			m++;
		}
		while (m == 0) 
		{ 
			printf("0"); 
			while (m == 0) 
			{
				printf("0");
			} 
		} 
	}
	int k = 10;
	while (m == 0) { printf("0"); while (m == 0) { printf("0"); }	while (m == 0) { printf("0"); while (m == 0) { printf("0"); while (m == 0) { printf("0"); while (m == 0) { printf("0"); while (m == 0) { printf("0"); while (m == 0) { if (1 == 1) { int f = 0; }printf("0"); } } } } } } }
	printf("%d", m);
	int factorial_of_k = recursive_function(k);
	printf("\nfactorial of %d = %d\n", k, factorial_of_k); 
	return 0; 
}
int aaaa;
/*
adhdzn *
zdgmndzgmgzcm *
fzgmgfzmbvm zgm *
*/